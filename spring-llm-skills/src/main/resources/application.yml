server:
  port: 8080

llm:
  # OpenAI ??
  openai:
    api-key: ${OPENAI_API_KEY:your-api-key-here}
    base-url: https://api.openai.com
    model: gpt-3.5-turbo
    timeout: 30000

  # ????????
  dashscope:
    api-key: ${DASHSCOPE_API_KEY:your-dashscope-key-here}
    base-url: https://dashscope.aliyuncs.com
    model: qwen-turbo

# ??
logging:
  level:
    com.example.llm: DEBUG