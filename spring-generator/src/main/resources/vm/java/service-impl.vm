package ${packageName};

import com.baomidou.mybatisplus.extension.service.impl.ServiceImpl;
import ${packageName.replace('.service.impl', '.entity')}.${entityName};
import ${packageName.replace('.service.impl', '.mapper')}.${mapperName};
import ${packageName.replace('.service.impl', '.service')}.${className.replace('Impl', '')};
import org.springframework.stereotype.Service;

/**
 * <p>
 * ${entityName} 服务实现类
 * </p>
 *
 * @author perfect-admin
 * @since ${.now}
 */
@Service
public class ${className} extends ServiceImpl<${mapperName}, ${entityName}> implements ${className.replace('Impl', '')} {

}
